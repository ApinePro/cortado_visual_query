import { QueryTreeDrawerDirective } from './query-tree-drawer.directive';

describe('QueryTreeDrawerDirective', () => {
  it('should create an instance', () => {
    const directive = new QueryTreeDrawerDirective();
    expect(directive).toBeTruthy();
  });
});
